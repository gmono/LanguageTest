
特征串为本程序核心

所有对象以其特征串为键值存储于二叉树中

每一个对象都有所属上级类对象集合和下属子对象集合
对上级类对象的引用可以适用于所有下级对象

原始语句会被处理成对象逻辑序列

特征串语义：
对对象的引用使用 [/id objfom …… /except objfom ……] 的形式
objfom是对象的特征串
except下的对象列表和所属对象被排除

对字的引用直接使用其文本 例如 [……]是吗


已经有过的语句序列全部存储于记忆池中

特征串是可以直接连接的